import { Component } from '@angular/core';

@Component({
  selector: 'customers',
  providers: [],
  template: require('./customers.template.html')
})

export class CustomersComponent {
    title = 'this is a the customers Componnt'
}
import { Component } from '@angular/core';

@Component({
  selector: 'users',
  providers: [],
  template: require('./users.template.html')
})

export class UsersComponent {
    title = 'this is a the users Componnt'

}